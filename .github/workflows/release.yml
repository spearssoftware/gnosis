name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --extra vectors
      - run: uv run gnosis validate
      - run: uv run gnosis build
      - name: Package output
        run: |
          cd output
          zip -r ../gnosis-${{ github.ref_name }}.zip *.json *.db *.usearch
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            output/people.json
            output/places.json
            output/events.json
            output/people-groups.json
            output/verse-index.json
            output/gnosis.db
            output/gnosis.usearch
            gnosis-${{ github.ref_name }}.zip
